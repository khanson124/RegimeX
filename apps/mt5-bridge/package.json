{
  "name": "@regimex/mt5-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "mailbox-cleanup": "tsx src/mailbox-cleanup-cli.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@regimex/config": "workspace:*",
    "@regimex/trading-engine": "workspace:*",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@regimex/eslint-config": "workspace:*",
    "@regimex/tsconfig": "workspace:*",
    "@types/node": "^22.10.7",
    "eslint": "^9.18.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
